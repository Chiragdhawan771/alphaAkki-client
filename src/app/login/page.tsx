import LoginPage from "@/components/auth/login-page";

/**
 * Page component that renders the login UI.
 *
 * Delegates rendering to the `LoginPage` component.
 *
 * @returns A JSX element containing the login page.
 */
export default function Login() {
  return <LoginPage />;
}
